{
  "name": "helman-backend-Python3.9",
  "dockerFile": "./Dockerfile",
  // "runArgs": [
  //   // pytestの実行コマンドを指定する
  //   "pytest"
  // ],
  "extensions": ["ms-python.python"], //, "ryanluker.vscode-coverage-gutters"], //, "emeraldwalk.RunOnSave"],
  "containerEnv": {
    "AWS_ACCESS_KEY_ID": "${localEnv:AWS_ACCESS_KEY_ID}",
    "AWS_SECRET_ACCESS_KEY": "${localEnv:AWS_SECRET_ACCESS_KEY}",
    "AWS_DEFAULT_REGION": "${localEnv:AWS_DEFAULT_REGION}"
  },
  // "workspaceMount":
  "mounts": [
    // "source=${localWorkspaceFolder},target=/app,type=bind",
    "source=/Users/ryosuke/.aws/credentials,target=/root/.aws/credentials,type=bind,consistency=cached"
  ],
  // "workspaceFolder": "/app", // "/workspaces/helman/stack/lambda_function",
  // "postCreateCommand": "pipenv install --dev",
  // "postAttachCommand": "pipenv install --dev",
  "settings": {
    // "terminal.integrated.shell.linux": "/bin/bash",
    // "python.pipenvPath": "/usr/local/bin/pipenv",
    // "python.pythonPath": "/usr/local/bin/python",
    // "python.pythonPath": "/root/.local/share/virtualenvs/lambda_function-j6Y7IpsY/bin/python",
    // "python.linting.pylintEnabled": false,
    // "python.linting.flake8Enabled": true,
    // "python.linting.flake8Args": [
    // "--ignore=E402,E501"
    // ],
    "python.formatting.provider": "autopep8",
    "python.formatting.autopep8Args": [
      "--ignore",
      "E402,E501",
      "--max-line-length",
      "150"
    ],
    // // "python.testing.pytestPath": "/root/.local/share/virtualenvs/lambda_function-j6Y7IpsY/bin/pytest",
    // "python.testing.pytestPath": "/usr/local/bin/pytest",
    // // "python.testing.cwd": "${workspaceFolder}",
    // "python.testing.pytestEnabled": true,
    // // "python.testing.pyTestArgs": ["."],
    // // ファイル保存時にテスト対象コードを自動的に探索
    // "python.testing.autoTestDiscoverOnSaveEnabled": true,
    // "python.testing.pytestArgs": ["--cov=.", "--cov-report", "xml"],
    // // エディタ上のカバレッジ表示設定
    // // ガター(ブレークポイント等が表示される場所)でのカバレッジ表示有無(default: true)
    // "coverage-gutters.showGutterCoverage": true,
    // // エディタ行でのカバレッジ表示有無(default: false)
    // "coverage-gutters.showLineCoverage": true,
    // // ルーラーでのカバレッジ表示有無(default: false)
    // "coverage-gutters.showRulerCoverage": true,

    "[python]": {
      "editor.formatOnSave": true
    }
    // "emeraldwalk.runonsave": {
    //   "commands": [
    //     {
    //       "match": ".*",
    //       "isAsync": true,
    //       "cmd": "echo 'I run for all files.'"
    //     }
    //   ]
    // }
  }
}
